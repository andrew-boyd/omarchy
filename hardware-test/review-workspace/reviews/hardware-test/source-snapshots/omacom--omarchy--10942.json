{
  "url": "https://github.com/omacom/omarchy/pull/10942",
  "origins": [
    "original-group-inventory",
    "expanded-family-ledger"
  ],
  "groups": [
    "older-macs"
  ],
  "prior_snapshots": [
    {
      "head_sha": "65255e08e9bac61abf0262835f04ad387c00fa47",
      "state": "open"
    }
  ],
  "repository": "omacom/omarchy",
  "number": 10942,
  "checked_at": "2026-09-21T11:56:57.732064+00:00",
  "title": "Document pre-2012 Intel Mac support",
  "author": "alkevintan",
  "head_sha": "65255e08e9bac61abf0262835f04ad387c00fa47",
  "base_sha": "5b91db503c904bbfc5f34bdaaa9c708814958f3d",
  "base_ref": "quattro",
  "updated_at": "2026-09-09T08:10:53Z",
  "state": "open",
  "body": "`manual/44-mac-support.md` covers T1 and T2 Macs but says nothing about the earlier Intel ones, and its install section promises Broadcom Wi-Fi \"automatically\" \u2014 which does not hold for them. This adds a section for the 2006\u20132011 machines.\n\nEverything here was verified end to end on an iMac7,1 (2007, Core 2 Duo T7300, Radeon HD 2400 XT / RV610, BCM4321), running Omarchy 4.0.0.alpha. Hyprland, Quickshell and the Omarchy shell all come up on it \u2014 the point of the section is the handful of things that quietly do not.\n\n## What it documents\n\n- **Graphics work, with little headroom.** Pre-GCN AMD GPUs on `radeon` top out at OpenGL ES 3.0, which is exactly Hyprland's floor since 0.50 dropped the legacy renderer. Hyprland fails to create a GLES 3.2 context, falls back to 3.0, and runs. Blur, shadows and animations cost ~28% of GPU throughput and it still holds 60 Hz at 1680x1050.\n- **No atomic modesetting.** Aquamarine detects this and falls back to legacy DRM on its own; hardware cursors still work.\n- **No Vulkan.** RADV needs `amdgpu`; `vulkan-radeon` installs an ICD that never enumerates a device.\n- **Night light does nothing.** `hyprsunset` sends a CTM over `hyprland-ctm-control-v1`, aquamarine submits CTMs only on the atomic path, and the connector reports `crtc doesn't support CTM`. `hyprctl` returns `ok` and the panel never changes.\n- **Wi-Fi needs `broadcom-wl-dkms`.** These ship a BCM4321 (`14e4:4328`) and the ISO carries no `b43` firmware, so the install has to be done over ethernet.\n- **Chromium flickers under native Wayland**, because its zero-copy compositing has neither explicit sync nor framebuffer modifiers to work with. XWayland cures it, and measured cheaper than `--disable-gpu-compositing` (20 vs 24 CPU-seconds per 20s of identical animation load) since that moves compositing onto a 2 GHz Core 2 Duo.\n- **`GDK_SCALE` then bites.** The shipped default is `omarchy_gdk_scale = 2` against `monitor_scale = \"auto\"`. On a low-DPI panel `auto` resolves to 1, and nothing notices the mismatch until an XWayland window draws at double size. `omarchy hyprland monitor scaling 1` sets both consistently.\n- **Most coding agents will not start.** `pi`, `opencode` and Claude Code now ship Bun-compiled binaries, and Bun has no target below x86-64-v2 \u2014 its \"baseline\" variant included. They die with `SIGILL` on a pre-2008 CPU: `popcnt` in pi's case, `pinsrd` (SSE4.1) in Claude Code's, confirmed by disassembling the faulting address from the core dump. `codex` works, and `pi` works from its npm package, which is still plain JS on Node.\n\n## Notes for reviewers\n\nThe Wi-Fi bullet describes hardware that #10936 teaches the installer to handle. The two are independent \u2014 this branch is docs only, off `quattro`, and can merge on its own \u2014 but if #10936 is rejected, that bullet should be reworded to describe a manual step rather than automatic behaviour.\n\nTwo of these look like they might deserve code changes rather than prose, and I have deliberately not attempted either here:\n\n- The `GDK_SCALE` default could be derived from the resolved monitor scale instead of defaulting to 2.\n- Omarchy could set the Chromium ozone platform based on whether the GPU has atomic KMS.\n\nBoth change behaviour for a whole class of machines, so they seemed worth documenting before automating.\n\n\ud83e\udd16 Generated with [Claude Code](https://claude.com/claude-code)\n\nhttps://claude.ai/code/session_01AUboE7GBC75XXqJC1pYUSk\n",
  "draft": false,
  "files": [
    {
      "sha": "5462b27c3ee79124cc0e276d3bef15358f557e7c",
      "filename": "manual/44-mac-support.md",
      "status": "modified",
      "additions": 18,
      "deletions": 0,
      "changes": 18,
      "blob_url": "https://github.com/omacom/omarchy/blob/65255e08e9bac61abf0262835f04ad387c00fa47/manual%2F44-mac-support.md",
      "raw_url": "https://github.com/omacom/omarchy/raw/65255e08e9bac61abf0262835f04ad387c00fa47/manual%2F44-mac-support.md",
      "contents_url": "https://api.github.com/repos/omacom/omarchy/contents/manual%2F44-mac-support.md?ref=65255e08e9bac61abf0262835f04ad387c00fa47",
      "patch": "@@ -37,10 +37,28 @@ It is necessary to disable Apple's Secure Boot in order to boot the bootable USB\n \n The installer detects Mac hardware and applies the needed fixes automatically: Broadcom Wi-Fi drivers and firmware, the SPI keyboard driver on the MacBook models that need it, and an NVMe suspend fix for those same models.\n \n+Keep an ethernet cable handy on the older models. The ISO carries no `b43` firmware, so the pre-2012 Macs have no wireless until the Broadcom driver is installed \u2014 see [Pre-2012 Intel Macs](#pre-2012-intel-macs) below.\n+\n ### Known Limitations\n \n Members of the community are constantly working on solutions to these challenges so if these are problematic for you, join #omarchy-on-other in our [Discord](https://discord.gg/tXFUdasqhY) and see if there's any up-to-date methods for resolving these.\n \n+#### Pre-2012 Intel Macs\n+\n+The 2006\u20132011 Intel Macs run Omarchy, but their graphics sit exactly at the edge of what Hyprland supports rather than comfortably inside it. Verified end to end on an iMac7,1 (2007, Core 2 Duo, Radeon HD 2400 XT), where Hyprland, Quickshell and the Omarchy shell all come up.\n+\n+- **Graphics work, with little headroom.** These machines have pre-GCN AMD GPUs (TeraScale and older) on the `radeon` driver, which tops out at OpenGL ES 3.0 \u2014 precisely Hyprland's floor since it dropped the legacy renderer in 0.50. Hyprland fails to create a GLES 3.2 context, falls back to 3.0, and runs normally from there. Blur, shadows and animations cost roughly 28% of GPU throughput on that iMac and it still holds 60 Hz at 1680x1050, so the shipped defaults are usable.\n+- **No atomic modesetting.** The `radeon` driver has no atomic KMS. Aquamarine detects this and falls back to the legacy DRM interface on its own \u2014 no environment variables needed \u2014 and hardware cursors still work.\n+- **No Vulkan.** RADV only drives GPUs bound to `amdgpu`. On these machines `vulkan-radeon` installs an ICD that never enumerates a device, so treat anything gated on Vulkan as unavailable.\n+- **Night light does nothing.** `hyprsunset` applies its temperature as a colour transform matrix, and aquamarine submits a CTM only on the atomic path. The request is accepted and silently dropped: `hyprctl` reports success, the temperature reads back, and the panel never changes.\n+- **Wi-Fi needs the Broadcom driver.** These models ship a BCM4321 (`14e4:4328`), which `broadcom-wl-dkms` covers. Install over ethernet, since the ISO has no `b43` firmware.\n+- **RAM is the real ceiling, not the GPU.** The compositor and shell together sit at roughly 400 MB. On a 3 GB machine that leaves plenty for the desktop and not much for a modern browser.\n+- **Chromium flickers under native Wayland.** Without explicit sync or framebuffer modifiers there is no way to know when a shared buffer is finished, so Chromium's zero-copy compositing draws half-written frames. Put `--ozone-platform=x11` and `--ozone-platform-hint=x11` in `~/.config/chromium-flags.conf` to run it through XWayland. `--disable-gpu-compositing` also cures the flicker but moves compositing onto the CPU, which measured about 20% more expensive on a Core 2 Duo \u2014 on these machines the CPU is scarcer than the GPU.\n+- **Check your scaling after that.** Omarchy defaults `omarchy_gdk_scale = 2` while the monitor scale is `\"auto\"`. On a low-DPI panel `auto` resolves to 1, and the mismatch only becomes visible once something runs under XWayland, which then draws at double size. `omarchy hyprland monitor scaling 1` sets both consistently; the change reaches apps at the next login.\n+- **Most coding agents will not start.** `pi`, `opencode` and Claude Code now ship as Bun-compiled binaries, and Bun has no build target below x86-64-v2 \u2014 even its \"baseline\" variant. On a pre-2008 Core 2 they die instantly with `SIGILL`. `codex` works. `pi` also works when installed from its npm package, which is still plain JavaScript on Node: `mise use -g npm:@earendil-works/pi-coding-agent`. Prefer the npm backend over mise's default for any agent that offers one.\n+\n+Full-disk encryption is worth a thought on these too: you cannot type the passphrase on a Bluetooth keyboard at boot, and Apple shipped one with most of these machines.\n+\n #### Devices with T1 Chip\n \n The Apple T1 chip was introduced in late 2016 and used exclusively in the first-generation MacBook Pro models with Touch Bar."
    }
  ],
  "comments": [],
  "reviews": [],
  "inline": []
}
