{
  "url": "https://github.com/omacom/omarchy/pull/12616",
  "origins": [
    "published-source-table"
  ],
  "groups": [],
  "prior_snapshots": [
    {
      "head_sha": "aa4630f75ce5dea81a386ebe09c3fbda6967f40d",
      "state": "open",
      "updated_at": "2026-09-20T10:17:45Z"
    }
  ],
  "repository": "omacom/omarchy",
  "number": 12616,
  "checked_at": "2026-09-21T11:57:57.354042+00:00",
  "title": "Stop counting Hyprland virtual outputs as external monitors",
  "author": "HritwikSinghal",
  "head_sha": "aa4630f75ce5dea81a386ebe09c3fbda6967f40d",
  "base_sha": "e38c1d1289252d2adb96372eeac48d02e489c5b7",
  "base_ref": "quattro",
  "updated_at": "2026-09-20T10:17:45Z",
  "state": "open",
  "body": "Fixes #8027.\r\n\r\n## What goes wrong\r\n\r\nDock the laptop, turn the laptop screen off (SUPER + CTRL + Delete, or Hardware -> Laptop Display), then unplug the external monitor. The laptop is left with no screen at all until the next login.\r\n\r\n## Why\r\n\r\nWhen the last enabled monitor goes away, Hyprland creates a placeholder output named FALLBACK (FallbackStateKeeper, src/state/FallbackState.cpp, since 0.56.0). Nothing is behind it, but it reports `disabled: false` and its name is not eDP/LVDS/DSI, so `omarchy-hyprland-monitor-external-active` answered \"an external monitor is active\".\r\n\r\nSix callers use that answer to decide whether turning the laptop screen back on is safe, and all six back off: `off()` and `recover()` in `omarchy-hyprland-monitor-internal`, `recover()` in `omarchy-hyprland-monitor-internal-mirror`, `enable_internal()` and the dock check in `omarchy-hyprland-monitor-clamshell`, and the poll in `omarchy-hyprland-monitor-watch`. Only the next login recovers it, because `omarchy-recover-internal-monitor.service` asks `omarchy-hw-external-monitors` instead, which reads the kernel.\r\n\r\n## The fix\r\n\r\nAsk the kernel too: a real display has a DRM connector, a placeholder has none. Fixing the helper fixes all six callers at once.\r\n\r\nExcluding the names FALLBACK and HEADLESS-* would also work today, but 0.55 called this same output HEADLESS-n and 0.56 renamed it, so the next rename would bring the bug back with nothing to notice it.\r\n\r\n`monitors all` stays, so a mirrored external keeps counting. Any connector status except `disconnected` counts, because a connector with no working detect (VGA, DVI-I) reports `unknown` for a display that is genuinely attached.\r\n\r\n## Measured on hardware\r\n\r\nDell XPS, Intel Panther Lake, eDP-1 plus a Dell AW3225QF on DP-1. Omarchy 4.0.0, Hyprland 0.56.2. Sampled 5 times a second across a real unplug:\r\n\r\n| | placeholder visible | toggle flag | laptop screen |\r\n|---|---|---|---|\r\n| before | 1.75s | stayed set | never came back on its own |\r\n| after | under 0.5s | cleared by the next sample | back immediately |\r\n\r\n## Tests\r\n\r\nNew `test/shell.d/monitor-external-active-test.sh`, 11 checks. Against the current helper it fails at the first one:\r\n\r\n    not ok - the FALLBACK output is not counted as an external monitor\r\n\r\nFull suite: 7 of 246 files fail, the same 7 that fail on a clean quattro checkout (see #7125).",
  "draft": false,
  "files": [
    {
      "sha": "935cd2cead87cb7e65894382275e76982d62210e",
      "filename": "bin/omarchy-hyprland-monitor-external-active",
      "status": "modified",
      "additions": 39,
      "deletions": 1,
      "changes": 40,
      "blob_url": "https://github.com/omacom/omarchy/blob/aa4630f75ce5dea81a386ebe09c3fbda6967f40d/bin%2Fomarchy-hyprland-monitor-external-active",
      "raw_url": "https://github.com/omacom/omarchy/raw/aa4630f75ce5dea81a386ebe09c3fbda6967f40d/bin%2Fomarchy-hyprland-monitor-external-active",
      "contents_url": "https://api.github.com/repos/omacom/omarchy/contents/bin%2Fomarchy-hyprland-monitor-external-active?ref=aa4630f75ce5dea81a386ebe09c3fbda6967f40d",
      "patch": "@@ -3,4 +3,42 @@\n # omarchy:summary=Returns true when Hyprland has an active external monitor\n # omarchy:hidden=true\n \n-hyprctl monitors all -j | jq -e '.[] | select(.name | test(\"^(eDP|LVDS|DSI)-\") | not) | select(.disabled == false)' >/dev/null 2>&1\n+# A name cannot tell a real display from one of Hyprland's virtual outputs:\n+# FallbackStateKeeper creates one called FALLBACK whenever the set of ENABLED\n+# monitors empties, and `hyprctl output create headless` makes HEADLESS-n. Both\n+# report disabled=false and match none of the internal-panel prefixes, so a name\n+# test counts them and the laptop panel never recovers. Ask the kernel instead:\n+# a real display has a DRM connector, a virtual output has none. Excluding the\n+# two names would work today, but 0.55 spelled FALLBACK \"HEADLESS-n\", and the\n+# next rename would put the bug back silently.\n+\n+drm_path=\"${OMARCHY_DRM_PATH:-/sys/class/drm}\"\n+\n+# Anything but `disconnected` counts: a connector with no working detect (VGA,\n+# DVI-I) reports `unknown` for a display that is genuinely attached. Virtual\n+# outputs have no status file at all, so the loop body never runs for them. The\n+# name needs no vetting: it is only ever used inside a quoted glob, where its\n+# characters are literal and cannot expand.\n+backed_by_connector() {\n+  local name=\"$1\" status\n+\n+  for status in \"$drm_path\"/card*-\"$name\"/status; do\n+    [[ -e $status ]] || continue\n+    [[ $(<\"$status\") == disconnected ]] || return 0\n+  done\n+\n+  return 1\n+}\n+\n+# `monitors all` rather than plain `monitors`, which omits mirrored externals. A\n+# hyprctl that fails or answers with nothing exits 1, as the previous jq did.\n+while read -r name; do\n+  [[ -n $name ]] || continue\n+  backed_by_connector \"$name\" && exit 0\n+done < <(hyprctl monitors all -j 2>/dev/null | jq -r '\n+  .[]\n+  | select(.disabled == false)\n+  | select(.name | test(\"^(eDP|LVDS|DSI)-\") | not)\n+  | .name' 2>/dev/null)\n+\n+exit 1"
    },
    {
      "sha": "dbeddb8d73e3828e1dca333d7abf35bded468c9b",
      "filename": "test/shell.d/monitor-external-active-test.sh",
      "status": "added",
      "additions": 131,
      "deletions": 0,
      "changes": 131,
      "blob_url": "https://github.com/omacom/omarchy/blob/aa4630f75ce5dea81a386ebe09c3fbda6967f40d/test%2Fshell.d%2Fmonitor-external-active-test.sh",
      "raw_url": "https://github.com/omacom/omarchy/raw/aa4630f75ce5dea81a386ebe09c3fbda6967f40d/test%2Fshell.d%2Fmonitor-external-active-test.sh",
      "contents_url": "https://api.github.com/repos/omacom/omarchy/contents/test%2Fshell.d%2Fmonitor-external-active-test.sh?ref=aa4630f75ce5dea81a386ebe09c3fbda6967f40d",
      "patch": "@@ -0,0 +1,131 @@\n+#!/bin/bash\n+\n+set -euo pipefail\n+\n+source \"$(cd -- \"$(dirname -- \"${BASH_SOURCE[0]}\")\" && pwd)/base-test.sh\"\n+\n+require_command jq\n+\n+test_tmp=$(mktemp -d)\n+trap 'rm -rf \"$test_tmp\"' EXIT\n+\n+stub_bin=\"$test_tmp/bin\"\n+drm_path=\"$test_tmp/drm\"\n+mkdir -p \"$stub_bin\"\n+\n+# The helper only ever asks `hyprctl monitors all -j`, so the stub answers that\n+# and nothing else -- an unexpected call is louder as a failure than as an empty\n+# reply.\n+cat >\"$stub_bin/hyprctl\" <<'SH'\n+#!/bin/bash\n+\n+if [[ $1 == \"monitors\" && $2 == \"all\" && $3 == \"-j\" ]]; then\n+  printf '%s' \"${OMARCHY_TEST_MONITORS_JSON:-[]}\"\n+  exit 0\n+fi\n+\n+printf 'unexpected hyprctl call: %s\\n' \"$*\" >&2\n+exit 1\n+SH\n+chmod +x \"$stub_bin/hyprctl\"\n+\n+write_connectors() {\n+  rm -rf \"$drm_path\"\n+  mkdir -p \"$drm_path\"\n+\n+  local connector state\n+  while (($#)); do\n+    connector=\"$1\"\n+    state=\"$2\"\n+    mkdir -p \"$drm_path/card0-$connector\"\n+    printf '%s\\n' \"$state\" >\"$drm_path/card0-$connector/status\"\n+    shift 2\n+  done\n+}\n+\n+external_active() {\n+  PATH=\"$stub_bin:$PATH\" \\\n+    OMARCHY_DRM_PATH=\"$drm_path\" \\\n+    OMARCHY_TEST_MONITORS_JSON=\"$1\" \\\n+    \"$ROOT/bin/omarchy-hyprland-monitor-external-active\"\n+}\n+\n+internal_only='[{\"name\":\"eDP-1\",\"disabled\":false}]'\n+\n+# The regression. Disabling the laptop panel while docked and then unplugging\n+# the external display empties Hyprland's ENABLED monitor set, so its\n+# FallbackStateKeeper conjures an output named FALLBACK. Counted as an external\n+# monitor, it stopped recovery re-enabling the panel and left the laptop with no\n+# display until the next login.\n+write_connectors eDP-1 connected DP-1 disconnected\n+if external_active '[{\"name\":\"eDP-1\",\"disabled\":true},{\"name\":\"FALLBACK\",\"disabled\":false}]'; then\n+  fail \"the FALLBACK output is not counted as an external monitor\"\n+fi\n+pass \"active external monitor helper ignores Hyprland's FALLBACK output\"\n+\n+write_connectors eDP-1 connected\n+if external_active '[{\"name\":\"eDP-1\",\"disabled\":false},{\"name\":\"HEADLESS-1\",\"disabled\":false}]'; then\n+  fail \"a headless output is not counted as an external monitor\"\n+fi\n+pass \"active external monitor helper ignores headless outputs\"\n+\n+# Everything below is behaviour the helper already had, pinned so the sysfs\n+# cross-check cannot quietly take it away.\n+write_connectors eDP-1 connected DP-1 connected\n+external_active '[{\"name\":\"eDP-1\",\"disabled\":false},{\"name\":\"DP-1\",\"disabled\":false}]' ||\n+  fail \"a connected external monitor is still reported as active\"\n+pass \"active external monitor helper finds a connected external monitor\"\n+\n+# A mirrored external is absent from plain `monitors`, which is why the helper\n+# asks `monitors all`; it is a real display and must keep counting.\n+write_connectors eDP-1 connected DP-1 connected\n+external_active '[{\"name\":\"eDP-1\",\"disabled\":false},{\"name\":\"DP-1\",\"disabled\":false,\"mirrorOf\":\"eDP-1\"}]' ||\n+  fail \"a mirrored external monitor is still reported as active\"\n+pass \"active external monitor helper sees mirrored externals\"\n+\n+# A connector with no working detect (VGA, DVI-I) reports `unknown` for a\n+# display that is genuinely attached. Demanding `connected` would stop off()\n+# disabling the panel on hardware where it always worked.\n+write_connectors eDP-1 connected VGA-1 unknown\n+external_active '[{\"name\":\"eDP-1\",\"disabled\":false},{\"name\":\"VGA-1\",\"disabled\":false}]' ||\n+  fail \"a connector reporting unknown is still treated as a real display\"\n+pass \"active external monitor helper counts connectors that report unknown\"\n+\n+# Hyprland can still list an output in the moment between the cable coming out\n+# and the monitor being removed. The kernel is already right; follow it.\n+write_connectors eDP-1 connected DP-1 disconnected\n+if external_active '[{\"name\":\"eDP-1\",\"disabled\":true},{\"name\":\"DP-1\",\"disabled\":false}]'; then\n+  fail \"an output whose connector has already gone is not counted\"\n+fi\n+pass \"active external monitor helper follows the kernel on a just-unplugged output\"\n+\n+write_connectors eDP-1 connected DP-1 connected\n+if external_active '[{\"name\":\"eDP-1\",\"disabled\":false},{\"name\":\"DP-1\",\"disabled\":true}]'; then\n+  fail \"an external monitor disabled on purpose is not reported as active\"\n+fi\n+pass \"active external monitor helper ignores monitors disabled on purpose\"\n+\n+for connector in eDP-1 LVDS-1 DSI-1; do\n+  write_connectors \"$connector\" connected\n+  if external_active \"[{\\\"name\\\":\\\"$connector\\\",\\\"disabled\\\":false}]\"; then\n+    fail \"$connector is treated as an internal panel, not an external monitor\"\n+  fi\n+done\n+pass \"active external monitor helper ignores common internal panel connectors\"\n+\n+write_connectors DP-1 connected\n+external_active '[{\"name\":\"DP-1\",\"disabled\":false}]' ||\n+  fail \"external-only systems still report an active external monitor\"\n+pass \"active external monitor helper still supports external-only systems\"\n+\n+# A compositor that cannot be asked is not a compositor with a monitor attached.\n+write_connectors eDP-1 connected DP-1 connected\n+if external_active ''; then\n+  fail \"an unanswered query does not invent an external monitor\"\n+fi\n+pass \"active external monitor helper treats an unanswered query as no external monitor\"\n+\n+if external_active \"$internal_only\"; then\n+  fail \"an internal-only session reports no external monitor\"\n+fi\n+pass \"active external monitor helper reports nothing on an internal-only session\""
    }
  ],
  "comments": [],
  "reviews": [],
  "inline": []
}
